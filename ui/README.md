# UI design reference

This directory contains the original static XAML design reference and its image assets.
It is intentionally not an executable project. The maintained application entry point is
`direct_module.csproj` at the repository root.
